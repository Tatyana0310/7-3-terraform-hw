---
- hosts: webservers
  become: yes
  tasks:

  - name: Install nginx
    apt:
        name: nginx
        state: present
        update_cache: yes


  - name: Ensure nginx service is running and enabled
    systemd:
        name: nginx
        state: started
        enabled: yes

